# my-app ── ブラウザで動く 3D ボクセルゲーム

このファイルはプロジェクトの正本です。会話履歴が消えても、ここを読めば状況がわかるように書いています。
**作業のたびに「現在地」と「次にやること」を更新してください。**

## このプロジェクトは何か

Three.js を使った、ブラウザで動く 3D ボクセルゲーム(マインクラフト風)です。
小学生の子どもと一緒に作って遊ぶことを想定しています。

- 公開ページ: https://urochikizashi.github.io/my-app/
- リポジトリ: https://github.com/urochikizashi/my-app (公開設定)

## 作りの前提(ここを壊さないでください)

- **単一ファイル構成**。ゲームは `index.html` 1 枚で完結します。HTML・CSS・JavaScript が全部その中です。
- **ビルド作業がありません**。npm install も webpack も使いません。ファイルを直せばそれが動くものです。
- **外部ファイルに依存しません**。テクスチャも効果音もコード内で生成しています。画像ファイルは置きません。
- **Three.js だけ CDN から読みます**。`<script type="importmap">` で `three@0.160.0` を jsdelivr から取得しています。バージョンを上げるときは動作確認を必ずしてください。
- JavaScript は `<script type="module">` の中に書かれています。**構文エラーが 1 つでもあると、モジュール全体が実行されません**。画面は表示されるのにボタンが一切効かない、という症状になります(実例は下の履歴を参照)。

### 中身の作り(知っておくと直しやすいこと)

- **ブロックの座標**。ブロック番号 n は n-0.5 〜 n+0.5 の範囲を占める(見た目の箱の中心が整数)。
  当たり判定もこの決まりにそろえてある(`collidesBox`)。昔はここが半マスずれていて、壁にめり込んでいた。
- **ワールドのデータ**は `Uint8Array` 1 本(0 が空気)。`getBlock` / `addBlock` / `removeBlock` を通して読み書きする。
- **描画**は 16×16 列ごとの「チャンク」単位。見えている面だけを 1 つのメッシュにまとめ、
  全ブロックの絵を 1 枚に並べた画像(アトラス)を貼っている。ブロックが変わるとそのチャンクだけ作り直す。
  1 ブロックずつ別のメッシュにすると、この広さでは重くて動かない。
  チャンクのメッシュは**近くだけ**作る(`updateChunks`。見える距離＋12 で作り、＋40 より遠くなると `releaseChunk` で片づける)。
  1 フレームに作るのは近い順に 3 個まで。最初の 1 回と、すぐ近くが空っぽのとき(復活など)はまとめて作る。遠くの松明も描かない。
- **見ているブロック**は光線をブロック 1 マスずつたどって探す(`raycastBlock`)。
- ブロックの種類は `BLOCKS`、持ち物のアイテムは `ITEMS`、敵の種類は `MOB_DEFS`、作れるものは `RECIPES` にまとまっている。
- 地形の種類は `biomeAt` / `BIOME_INFO`、高さは `surfaceHeight`。ダンジョンの場所と大きさは `DUNGEONS`(`generateDungeon` → `buildDungeonRoom` で 3 つ作る。強さと宝は `BOSS_TIERS`)。
  ボスの動きは `updateBoss`(種類ごとの数字は `BOSS_TIERS`、見た目は `GOLEM_SKINS`)、マップは真上から見たワールドと同じ大きさ(1 マス 1 ピクセル)の絵を作り、変わった列だけ描き直している(`markMapDirty`)。
  行ったことのある場所は `explored`(1 マス 1 ビットでセーブ)。
- **地上の建物**は `STRUCTURE_TYPES`(地形ごと)と `generateStructures`。洞窟とダンジョンを作ったあとに建てる。
  建てる前に `prepareSite` で地ならしし、`placeLootChest` でチェストの中身を決める。
  中身を決める時点では `storageChests` がまだ無いので、`structureLoot` にためておき、あとで `applyStructureLoot` で入れる。
- **地下の建物**は `generateUnderground`(`UNDERGROUND_PLAN` に種類・数・深さ)。一覧は `undergroundSites`、見つけたかは `sitesFound`(セーブする)。
  古代遺跡 `buildAncientRuins`・廃坑 `buildMineshaft`/`mineCorridor`・敵の湧くかご `buildSpawnerRoom`(動きは `updateSpawners`)。
  置き場所は `undergroundSpotFree` で、地表・ボスの部屋と階段・地上の建物・ほかの地下の建物・溶岩からはなす。
  **生成のいちばん最後に呼ぶこと**。`worldRandom` の順番が変わると、前からあるワールドの地上の建物の場所がずれる。
- **水と溶岩は流れる**。源は `WATER` / `LAVA`、流れた先は `FLOW_WATER` / `FLOW_LAVA`(いきおいは `fluidLevels`)。
  2つのちがいは `FLUIDS` の表(横に何マスまで・何回に1回動くか)だけ。
  湖ぜんぶを毎秒計算すると重いので、`addBlock` / `removeBlock` がさわった所のまわりだけ `activeFluid` に積み、
  `updateFluids` が 0.25 秒ごとに少しずつ処理する。いきおいが弱くなった直後は広げない(でないと引いた水をまた押し出す)。
  溶岩は水にふれると固まる(`lavaMeetsWater`)。源(`LAVA`)なら黒曜石(`OBSIDIAN`)、流れなら石。
  バケツの水を溶岩に直接かけたときも同じ(`useBucket`)。黒曜石はツルハシの段 4(ダイヤ)でしか掘れない。光って見えるのは `lavaMaterial` の emissive。
  水面・溶岩のゆらぎは `addWave`(材質の onBeforeCompile)で、上を向いた面だけを時間で上下させている。
- クラフトは `craftGrid`(3×3)に並べた形を `RECIPES` と照らし合わせる。`shaped` は並べ方(左右反転も OK)、`shapeless` は形を問わない。
  レシピ本は、材料を一度でも手に入れた(`seenItems`)作り方だけ表示する。
- チェストとかまどの中身は `storageChests` / `furnaces`(ブロックの座標 "x,y,z" ごと)、育っている作物と苗は `growingBlocks`、
  地面に落ちているアイテムは `droppedItems`(これだけはセーブしない)。

## 現在地 (2026-09-22 時点)

| ブランチ | 状態 |
| --- | --- |
| `main` | 遊べる状態。公開ページはこれ。PC でマウスとキーボードで操作する |
| `mobile-support` | タッチ操作の実装が入っている。**実機未検証**。仕上げればタブレットで遊べる |

`main` に実装済みのもの:

- **広いワールド**。新しいワールドは 321×321 ブロック(`NEW_WORLD_SIZE`)。広さはセーブに `worldSize` として残し、
  それが無い前からのセーブは 161×161 のまま読む(広さが変わると `worldRandom` の順番がずれて木や洞窟の場所が全部変わるため)。
  洞窟・溶岩・クモの巣・建物・地下の建物の数は `scaled()` で面積に合わせて増やし、ボスの部屋は `WORLD_SCALE` に合わせて遠くする。
  161×161 のときは前とまったく同じワールドができるようにしてある
- **5 種類の地形**。草原・森(木が多い)・砂漠(砂とサボテン、さわると痛い)・雪原(雪、とがった木、凍った湖)・山(てっぺんは雪)。
  湖(水の中はゆっくり、スペースで浮かぶ)、洞窟のクモの巣(遅くなる・掘ると糸)。1 段の段差は自動で上る
- **ボス 3 体**。地下の石レンガのダンジョン 3 か所(`BOSS_TIERS` / `DUNGEONS`)。前のボスを倒すと次が目を覚まし、その部屋がマップに出る。
  1: ゴーレム(-30 m・体力 300)、2: マグマゴーレム(-34 m・450・岩が痛い)、3: やみのゴーレム(-38 m・650・仲間を呼ぶ・水が効かない)。
  パンチ・岩投げ・地面たたきの衝撃波(ジャンプでよける)。体力半分で怒る。
  **弱点は背中の光るコア(後ろから当てると3倍。ゆっくりしか振り向けない)と水(バケツの水で5秒しびれる。3体目には効かない)**。
  倒すとその部屋の宝箱が開く(ゴーレムの剣 20 → マグマの剣 26 → やみの剣 34・やみのよろい)。途中で倒されたらボスは眠りにもどる
- **マップ**。右上にミニマップ(赤い点が敵)、M キーで全体マップ。行ったことのある場所だけ見える。
  ダンジョンは入口の近くまで行って見つけると、はじめて 💀 と 🚪 が出る
- **地上の建物**。地形に合わせて 7〜12 個(321×321 では 4 倍)。草原=小屋・井戸、森=廃屋、砂漠=ピラミッド(地下に宝物庫)、
  雪原=かまくら、山=見張り塔。それぞれ中身の違うチェストが入っている。1種類 3 つ(4 倍)まで、28 マス以上はなして建てる
- **水が流れる**。源から低い方へ、横は3マスまで広がる。源を取りのぞくと引いていく。水面はゆらゆら波打つ
- **水中**。もぐると画面が青くなり、息(🫧 10秒)が減る。切れると 1.5 秒ごとにダメージ。顔を出すと回復
- **溶岩**。地下深くの大きな池と、洞窟の小さな溶岩だまり。さわると大ダメージで動きが遅くなる。
  水と同じ仕組みでゆっくり流れ、水にふれると石になる(溶岩の源は**黒曜石**になる)。バケツでくめる
- **黒曜石とダイヤのツルハシ**。黒曜石はダイヤのツルハシでしか掘れない(約 4 秒)。異世界ゲートの材料にする予定
- **地下の建物**(数は 161×161 のとき。321×321 では 4 倍)。古代遺跡 3 つ(-18〜-26 m・大広間の祭だんに宝箱・ひびの入ったレンガの奥にかくし部屋)、
  廃坑 3 つ(-9〜-15 m・木の柱の通路が枝分かれ・洞窟の上は板の橋)、敵の湧くかご 6 つ(近くにいると敵が出る・壊すと止まる)。
  中に入ると「見つけた！」と出てマップに印がつく
- **落下ダメージ**。3マスより高いところから落ちると、1マスにつきハート半分。水・はしごではダメージなし
- **掘るのに時間がかかる**。左クリックを押しているあいだだけ進み、ブロックにひびが入る(`updateMining` / `miningSeconds`)。
  かたいほど遅く、良いツルハシほど速い
- **道具の耐久**。ツルハシ・剣・クワ・弓・盾・よろいは使うと減り、0 で壊れる(`wearItem` / `usesLeft`)。
  持ち物の枠に細いバーで残りを表示。同じ道具を 2 つ並べると直せる(`findRepair`)
- 地形生成、ブロックの破壊と設置、剣での攻撃、体力とリスポーン
- **敵**。ゾンビ(殴る)、ハスク(砂漠のゾンビ・昼も燃えない)、ストレイ(雪原のスケルトン・矢で遅くなる)、
  クリーパー(爆発して地形を壊す)、クモ(速い・壁を登る・一番よく出る・糸を 1〜3 本落とす)、
  スケルトン(離れて弓矢を撃つ・壁越しには撃てない)、スライム(地下だけ・跳ねる・大きいと分裂)。
  敵にも当たり判定があり、壁をすり抜けない。1 段はジャンプで越える
- **動物**。ブタ・ウシ・ニワトリ(`ai: 'passive'`)。明るい草の上に湧き、うろうろするだけ。
  なぐると逃げる。生肉(かまどで焼ける)・羽根(矢の材料)を落とす。
  小麦を持つとついてきて、小麦をあげた 2 匹がそろうと赤ちゃんが生まれる(`tryBreed`)。敵の数には数えない
- **昼と夜**。1 日 5 分(昼 約 3 分・夜 約 2 分)。新しいワールドは朝から。夜は空が暗くなり、たいまつ光がともる
- **敵は暗い場所にだけ出る**。夜の地上(最初は 4 体、4 体倒すごとに +1、最大 10 体)と、
  地面から 5 m 以上もぐった地下(深いほど増え、最大 8 体)。昼の地上には出ず、朝になると明るい場所の敵は燃えて消える。
  松明の 8 マス以内には出ない。遠く離れた敵や、45 秒以上近づけない敵は消えて湧き直す
- **深いほど敵が強い**。出てきた場所の深さで、体力 最大 3 倍・攻撃力 最大 2 倍・速さが上がり、大きく赤っぽくなる
- **持ち物は空から始まる**(素手・攻撃 3)。ホットバー 8 枠 + カバン 16 枠(大きなカバンで 32 枠)。
  E の画面でクリックしてつかむ → 置く、ゴミ箱、Q キーで地面に捨てる(近づくと拾える)、チェスト(24 枠)にしまえる
- **アイテム**。ツルハシ(手に持っているときだけ効果。石=木、鉄=石、金とダイヤ=鉄のツルハシが必要)、
  剣(手に持つと攻撃力が上がる)、防具(持っていればダメージ -30% / -50% / -65%)、
  食べ物(りんご・腐った肉・焼いた肉・パン・金のりんご、右クリックで回復)、松明(置くと明るく、敵が出ない)。
  建物: 木の板・色つきブロック 6 色・はしご(登れる)・ドア(開け閉め)・ランプ(光る)。
  道具と武器: 弓と矢・盾(右クリックを押しているあいだダメージ 1/5)・バケツ(水を運ぶ)・TNT(松明で点火)。
  生活: ベッド(復活地点・夜を飛ばす)・かまど(砂→ガラス、原木→木炭、腐った肉→焼いた肉)・チェスト。
  農業: 草むら→種、クワで耕して植えると小麦が育つ(水が近いと早い)、苗→木、花→染料。
  敵は腐った肉・糸・火薬(クリーパー)・矢と石炭か鉄(スケルトン)・スライムボールを落とす
- **クラフト**。E キー / 作業台を右クリック で開く。材料をマスに並べる形で作るもの が決まる(マインクラフト式)。
  ふだんは 2×2、作業台の近くでは 3×3。レシピ本のカードをクリックすると材料を自動で並べる。はじめは 原木→木の板→作業台
- 自然回復。6 秒間ダメージを受けなければハートが少しずつ戻る。復活直後は 3 秒間無敵
- **地下探検**。地下 40 m まで掘れ、最下層に壊せない岩盤がある。
  深さに応じて鉱石が出る(石炭 / 鉄 -6 m〜 / 金 -18 m〜 / ダイヤ -28 m〜)。
  潜るほど暗くなり、カメラに追従するたいまつ光が強くなる。
  画面左上に現在の深さと最深記録、持っている防具とツルハシを表示する
- **洞窟**。地下に毎回ランダムな洞窟(曲がりくねったトンネルと広い空洞)が生成される。
  地表にいくつか入口が開き、壁に鉱石が露出していることもある
- **セーブ**。ブラウザの localStorage に自動保存する。ESC で一時停止したとき、30 秒ごと、
  ページを閉じるとき。一時停止画面に手動セーブと「新しいワールド」のボタンがある。
  保存対象は地形(シードと変更したブロック)、位置と向き、体力、持ち物、撃破数、最深記録、時刻。
  どのボスを倒したか・どの宝箱を開けたか(`bossesDefeated` / `chestsLooted`。版 5 の古いセーブは 1 体目の記録として読む)、
  チェストとかまどの中身、カバンの大きさ、行った場所、見つけたダンジョン(`dungeonsFound`)、
  ベッドの復活地点、レシピ本の見つけた材料も保存する。地面に落ちているアイテムは保存しない。
  セーブの版は 5(草花とクラフトの仕組みを足したときに上げた)。版 4 以前のセーブは読み込まない。
  敵は保存せず、再開時に新しく湧く
- **テスト用フック**。URL に `?debug` を付けると、コンソールから `game`(敵の一覧、持ち物、時刻の変更、
  ブロックの削除など)を触れる。ヘッドレスブラウザでの自動確認にも使っている(`puppeteer-core` と
  `~/.cache/puppeteer` の Chrome。ポインターロックが外れるとゲームが止まるので、テスト中は
  `document.pointerLockElement` を見て取り直し、死なないように体力を入れ直すのがコツ)

## 次にやること(候補)

0. **2026-09-22 に決めた大きな計画の続き**(1 と 2 は済んだ)。
   1. ~~黒曜石とダイヤのツルハシ~~ 2. ~~地下の古代遺跡・廃坑・敵の湧くかご~~
   3. 新しい敵を何体か(コウモリ・ウィッチ・遺跡の番人など)
   4. 異世界ゲート(黒曜石のわく＋火打ち石)と異世界。ワールドが大きな箱 1 つ前提なので、
      箱をもう 1 つ持って入れ替える大工事。セーブの版も上げる
   5. ゴーレム以外のボス(大グモの女王・ファラオのミイラ・異世界のドラゴンなど)。遺跡や異世界の奥に置く
1. **`mobile-support` を実機で検証して仕上げる**。仮想スティック、攻撃/設置/ジャンプのボタン、
   PointerLock が無い環境での視点回転が実装済み。タブレットで開いて動くか確認する。
   ただし `main` は地下探検の実装で大きく変わったため、そのまま合流はできない。
   差分を確認し、タッチ操作の部分だけを現在の `main` に移す作業が要る。
2. **遊んでみて数字を調整する**。昼夜の長さ、敵の数、深さによる強さ、剣の攻撃力、ゴーレムの強さは
   ブラウザでの自動テストでしか確かめていない。実際に遊んで、難しすぎ/簡単すぎを直す。
3. **実際の PC で重さを確かめる**。321×321 に広げた(描くのは近くだけにした)。自動テストはソフトウェア描画なので、
   本物の画面での FPS は未確認。重ければ `VIEW_DISTANCE`(描く距離)や洞窟の本数を減らす。
4. **さらに広げる候補**。動物(ブタ・ニワトリ。生肉→焼き肉)、宝の地図、看板、
   掘るのに時間がかかる仕組み(ツルハシで速くなる)、エンチャント。
5. 実装を変えたら `README.md` の操作方法も一緒に更新すること。

## 作業のしかた

### 確認の手順

ローカルで見るときは、リポジトリのフォルダで簡易サーバーを立てます。
`file://` で直接開くと、モジュールの読み込みが CORS で失敗します。

```
python3 -m http.server 8000
```

`main` に push すると GitHub Actions が走り、1 分ほどで公開ページに反映されます。
反映されないときは Actions タブで結果を確認し、ブラウザは Shift を押しながら再読み込みします。

### 絶対に守ること

**作業の区切りごとに commit して push する。**
Codespace は既定で 30 日放置すると削除され、push していない変更は一緒に消えます。
2026-09-11 には、これを怠って 345 行の実装が消える寸前になりました(下記)。

`main` を壊したくない実験は、ブランチを切ってください。

```
git switch -c 作業の名前
git push -u origin 作業の名前
```

### 会話を残したいとき

セッションの記録はコンテナの中にしかなく、Rebuild や削除で消えます。
残す価値のあるやり取りは書き出して、リポジトリに入れてください。

```
/export docs/YYYY-MM-DD-作業ログ.md
```

書き出したファイルには画面に出た内容がそのまま入ります。公開リポジトリなので、
push する前に中身を一度確認してください。

## 子どもと一緒に作業するときの注意

- リポジトリは**公開設定**です。書いたコードもコミットの名前も誰でも見られます。
  コミットは保護者のアカウントで行ってください。
- 説明は平易な言葉で。専門用語を使うときは、必ず一度かみ砕いてください。
- 1 回の作業を短く区切ってください。「地面を掘る」「階段を置く」「暗くする」のように、
  毎回目に見える変化が出る単位に分けると続きます。
- Codespaces の無料枠は月 120 コア時間です。2 コアの環境で約 60 時間にあたります。
  **終わったら Codespace を停止してください。** 止め忘れだけが枠を減らします。

## これまでの経緯

| 日付 | 出来事 |
| --- | --- |
| 2026-09-11 | Codespace 上で GitHub Copilot CLI と作成。ゲーム本体、クリーパー、ゾンビを実装 |
| 2026-09-11 | モバイル対応 345 行を実装したが、**コミットせずに終了** |
| 2026-09-13 | `index.html` 1339 行目の閉じ括弧が 1 つ多く、ゲームが起動しない不具合が判明して修正 |
| 2026-09-13 | 未コミットのモバイル対応を Codespace から救出し、`mobile-support` として保存 |
| 2026-09-13 | 直後に元の Codespace が削除された。会話の記録はここで失われた |
| 2026-09-13 | 地下探検(地下 40 m・鉱石 4 種・たいまつ光)と敵の再出現を実装 |
| 2026-09-13 | 洞窟の自動生成、localStorage へのセーブ、敵の強さ調整、WASD の向き修正 |
| 2026-09-13 | 再発防止として `.devcontainer/devcontainer.json` とこのファイルを追加 |
| 2026-09-13 | 「移動できない」不具合を修正。原因は小数の誤差(4.6 - 1.6 = 2.9999…)で足元のブロックに埋まったと判定されること、木の中に出現すること、その位置が自動セーブされること。敵が気づく距離も広げた(ゾンビ 20・クリーパー 16)。セーブの版を 2 に上げたので古いセーブは読み込まれない |
| 2026-09-13 | 「敵が全然出てこない」との報告を受け、出現の仕組みを作り直し。昼と夜、暗い場所(夜・地下)でだけ出る敵、深いほど強い敵、空から始まる持ち物、クラフトと作業台を追加。`?debug` のテスト用フックを追加 |
| 2026-09-13 | 作業の記録をイシュー #1 に残した |
| 2026-09-13 | ワールドを 29×29 から 161×161 に拡大(描き方をチャンク方式に作り直し)。壁にめり込む不具合を修正(見た目と当たり判定の半マスのずれ、敵に横の当たり判定が無かった)。敵にクモ・スケルトン・スライムを追加。アイテムにツルハシ・食べ物・防具・松明、持ち物にカバンを追加 |
| 2026-09-13 | 「糸が手に入らない」との報告を受け、クモを出やすくして糸を 1〜3 本落とすように、洞窟にクモの巣を追加。地形を 5 種類(草原・森・砂漠・雪原・山)と湖に。ハスク・ストレイ、地下ダンジョンのボス(ゴーレム)と宝箱、ミニマップと全体マップ(M キー)を追加 |
| 2026-09-13 | 作業の記録をイシュー #1 に追記 |
| 2026-09-13 | 「最初からボスの場所がわかるのは面白くない」「持ち物がいっぱいになる」「作業台で組み合わせて作りたい」との要望。マップを行った場所だけ見えるように(ダンジョンは入口を見つけると出る)。Q キーで捨てる・ゴミ箱・チェスト・大きなカバン。クラフトを 3×3 マスに並べる方式にしてレシピ本を追加。建物のブロック・弓矢・盾・バケツ・TNT・ベッド・かまど・農業を追加 |
| 2026-09-20 | 動物(ブタ・ウシ・ニワトリ)、生肉と羽根、小麦でのえさやりと繁殖を追加 |
| 2026-09-20 | 掘るのに時間がかかる仕組み(ひび割れ表示)、道具の耐久と修理を追加。`.slot` の `hidden` が CSS の `display:flex` に負けていて、2×2 のクラフトでも 9 マス見えていた不具合も修正 |
| 2026-09-20 | 溶岩(地下の池・ゆっくり流れる・水にふれると石になる)、水中の息と青い画面、落ちたアイテムを「入るぶんだけ拾う」ように修正(持ち物がいっぱいのときは画面に知らせる)を追加 |
| 2026-09-20 | 「草原や砂漠に建築物がほしい」「水が流れてほしい」「ゴーレムが強すぎる」「落下ダメージもほしい」との要望。地上の建物 6 種類(小屋・井戸・廃屋・ピラミッド・かまくら・見張り塔)と中身の入ったチェスト、流れる水と波打つ水面、ゴーレムの弱点 2 つ(背中のコア・水)、落下ダメージを追加。セーブの版は 5 のまま(地形はシードから作り直され、建物が増えるだけなので、前のワールドもそのまま遊べる) |
| 2026-09-20 | 「ツルハシを一度使うとずっと使っている判定になる」との報告。ツルハシを**手に持っているときだけ**効くように変更(`pickaxeTier` を `heldItemDef` ベースに)。耐久も手に持っている枠だけ減る。持っているのに手に持っていないときは、お知らせと画面左上の表示で教える |
| 2026-09-20 | 「捨てると耐久が回復する」「まとめて作りたい」「ゴーレムのあとに別のボスも」との要望。落ちているアイテムが残り回数を持つように修正、Shift＋クリックでまとめてクラフト、ボスを 3 体に(マグマゴーレム・やみのゴーレム。倒すと次が目覚め、その部屋がマップに出る) |
| 2026-09-22 | 「異世界に行きたい(溶岩に水→黒曜石→ダイヤのツルハシ→ゲート)」「掘ると古代遺跡があるような地下」「敵とボスの種類を増やしたい」との要望。5 段階の計画を立て(「次にやること」の 0)、1 と 2 を実装。溶岩の源に水で黒曜石・ダイヤのツルハシ、地下の古代遺跡・廃坑・敵の湧くかご。セーブの版は 5 のまま(地下の建物は生成の最後に足すので、前のワールドにもそのまま出てくる) |
| 2026-09-22 | 「ワールドをもっと広げたい」との要望。新しいワールドを 321×321(面積 4 倍)に。チャンクを近くだけ作る方式に変え、読み込みはむしろ速くなった。前からのセーブは 161×161 のまま遊べる(セーブに `worldSize` を足した。版は 5 のまま) |

この経緯が、上の「絶対に守ること」の理由です。
**倉庫(GitHub)に入れたものだけが残り、机(Codespace)の上のものは机ごと消えます。**
