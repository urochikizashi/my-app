@AGENTS.md

## Claude Code 固有のメモ

- 応答は日本語で行うこと。
- 子どもと一緒に作業している場合がある。専門用語はかみ砕いて説明すること。
- `index.html` を編集したら、返す前に JavaScript の構文が壊れていないか確認すること。
  モジュール部分を抜き出して `node --check` に通すのが確実。
  構文エラーがあると画面は出るのにボタンが一切効かない、という分かりにくい壊れ方をする。
- 作業の区切りでは、コミットと push を自分から促すこと。
